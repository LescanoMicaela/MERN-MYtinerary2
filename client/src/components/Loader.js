import React from 'react';
import loaderImg from '../assets/images/loader.gif';

const Loader = props => (
  <div>
    <img
    alt='loader icon'
    src={loaderImg} />
  </div>
);

export default Loader;
